defaultRotation
{
	antares
    usstremor
	forlorn
	parpax
	perseus
	plat23
	if numPlayers > 5 spacetracks
	station15
	if numPlayers > 9 thunder
	if numPlayers > 3 vega
	yocto
	if numPlayers < 15 chasm
}
